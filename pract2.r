data=data.frame(x1=c(2,3,4,5,6),x2=c(70,75,80,85,90),y=c(50,55,60,65,70))
test_data=data.frame(x1=c(7,8),x2=c(95,90))
model=lm(y~x1+x2,data)
predict(model,test_data)
install.packages("MASS")
library("MASS")
data("Boston")
head(Boston)
str(Boston)
dim(Boston)
summary(Boston)
cor(Boston)
install.packages("corrplot")
library(corrplot)
library(corrplot)
x=cor(Boston)
corrplot(x)
set.seed(100)
sample_rows<-sample(1:nrow(Boston),0.9*nrow(Boston))
train<-Boston[sample_rows,]
test<-Boston[-sample_rows,]
model<-lm(medv~.,data=train)
prediction<-predict(model,newdata=test)
comparison<-data.frame(Actual=test$medv,Predicted=prediction)
comparison
plot(test$medv,prediction,xlab = "Actual Price",ylab = "Predicted Price",main = "Actual vs Predicted")
abline(0,1,col="red")
